rules_version = '2';
// TripApp — Storage security rules (for synced photos & background image).
// Paste into Firebase console: Build > Storage > Rules > Publish.
service firebase.storage {
  match /b/{bucket}/o {
    match /couples/{coupleCode}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
